pub fn init_vector() -> (Vec<i32>, Vec<i32>) {
    // let mut a: Vec<i32> = Vec::new();
    // a.push(1);
    // a.push(2);
    // a.push(3);
    let a = vec![1, 2, 3];
    let b = vec![1, 2, 3];
    (a, b)
}
